import 'package:flutter/material.dart';

class HYFavorScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('IDC工作台'),
        ),
        body: Center(child:Text("IDC工作台"),)
    );
  }
}
